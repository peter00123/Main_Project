<?xml version="1.0" encoding="utf-8"?>
<!--
=============================================================================
FILE: res/layout/fragment_transfer.xml
=============================================================================
OBJECTIVE:
  Full-screen transfer progress layout. Shows:
    - Remote device avatar + name
    - File being transferred (name + size)
    - Circular progress ring with percentage in centre
    - Speed and ETA labels
    - Accept/Decline buttons (receiver, AWAITING_USER state)
    - Cancel button (both sides, while active)
    - Done button + result icon (after terminal state)
=============================================================================
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:paddingHorizontal="24dp">

    <!-- ── Header: "Sharing with" label ── -->
    <TextView
        android:id="@+id/tv_sharing_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="@string/sharing_with"
        android:textAppearance="@style/TextAppearance.Material3.LabelLarge"
        android:textColor="?attr/colorOnSurfaceVariant"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Remote device avatar ── -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cv_remote_device"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_marginTop="12dp"
        app:cardCornerRadius="36dp"
        app:cardBackgroundColor="@color/avatar_green"
        app:cardElevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/tv_sharing_label"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_device_initials"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            tools:text="GS"/>
    </androidx.cardview.widget.CardView>

    <!-- ── Remote device name ── -->
    <TextView
        android:id="@+id/tv_device_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
        android:textColor="?attr/colorOnSurface"
        tools:text="Galaxy S24 Ultra"
        app:layout_constraintTop_toBottomOf="@id/cv_remote_device"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Circular progress ring ── -->
    <!-- Uses a determinate ProgressBar styled as a ring via layer-list drawable -->
    <ProgressBar
        android:id="@+id/progress_ring"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_marginTop="32dp"
        android:max="100"
        android:progress="0"
        android:progressDrawable="@drawable/progress_ring"
        android:rotation="-90"
        tools:progress="65"
        app:layout_constraintTop_toBottomOf="@id/tv_device_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Percentage label overlaid on the progress ring -->
    <TextView
        android:id="@+id/tv_progress_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Material3.HeadlineMedium"
        android:textColor="?attr/colorOnSurface"
        android:textStyle="bold"
        tools:text="65%"
        app:layout_constraintTop_toTopOf="@id/progress_ring"
        app:layout_constraintBottom_toBottomOf="@id/progress_ring"
        app:layout_constraintStart_toStartOf="@id/progress_ring"
        app:layout_constraintEnd_toEndOf="@id/progress_ring"/>

    <!-- Result icon (checkmark / error) — replaces the ring when done -->
    <ImageView
        android:id="@+id/iv_result_icon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:visibility="gone"
        android:contentDescription="@string/transfer_status"
        app:layout_constraintTop_toTopOf="@id/progress_ring"
        app:layout_constraintBottom_toBottomOf="@id/progress_ring"
        app:layout_constraintStart_toStartOf="@id/progress_ring"
        app:layout_constraintEnd_toEndOf="@id/progress_ring"
        tools:src="@drawable/ic_check_circle"
        tools:visibility="visible"/>

    <!-- ── File name ── -->
    <TextView
        android:id="@+id/tv_file_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="middle"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        android:textColor="?attr/colorOnSurface"
        tools:text="vacation_photo_2024.jpg"
        app:layout_constraintTop_toBottomOf="@id/progress_ring"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── File size + speed row ── -->
    <LinearLayout
        android:id="@+id/layout_stats"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/tv_file_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_file_size"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="3.2 MB"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="6dp"
            android:text="•"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"/>

        <TextView
            android:id="@+id/tv_speed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="?attr/colorPrimary"
            tools:text="2.1 MB/s"
            tools:visibility="visible"/>
    </LinearLayout>

    <!-- ETA label -->
    <TextView
        android:id="@+id/tv_eta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:visibility="gone"
        android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="12s left"
        tools:visibility="visible"
        app:layout_constraintTop_toBottomOf="@id/layout_stats"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Status label ── -->
    <TextView
        android:id="@+id/tv_status"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="Transferring…"
        app:layout_constraintTop_toBottomOf="@id/tv_eta"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Accept / Decline Buttons (receiver side) ── -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_accept"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginBottom="8dp"
        android:text="@string/accept"
        android:visibility="gone"
        app:cornerRadius="26dp"
        app:layout_constraintBottom_toTopOf="@id/btn_decline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_decline"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginBottom="32dp"
        android:text="@string/decline"
        android:visibility="gone"
        app:cornerRadius="26dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Cancel Button (in-progress) ── -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_cancel"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:text="@string/cancel"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ── Done Button (terminal states) ── -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_done"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginBottom="32dp"
        android:text="@string/done"
        android:visibility="gone"
        app:cornerRadius="26dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
