<?xml version="1.0" encoding="utf-8"?>
<!--
    nav_header.xml
    Header section of the burger (drawer) menu.
    Shows the app logo and name at the top of the sliding panel.
    Used by: res/layout/activity_main.xml (NavigationView headerLayout)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:background="@color/primary"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="24dp">

    <ImageView
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_share_logo"
        android:contentDescription="App logo"
        android:tint="@android:color/white"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@android:color/white"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Secure Document Sharing"
        android:textColor="#CCFFFFFF"
        android:textSize="12sp" />

</LinearLayout>
