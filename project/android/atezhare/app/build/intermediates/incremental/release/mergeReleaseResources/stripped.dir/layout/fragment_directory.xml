<?xml version="1.0" encoding="utf-8"?>
<!--
    fragment_directory.xml
    File directory layout with RecyclerView, FAB (+) for adding files,
    and a Send button at the bottom to proceed to SendActivity.
    Used by: ui/directory/DirectoryFragment
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Header label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select files to share"
            android:textSize="16sp"
            android:textColor="@color/text_secondary"
            android:padding="16dp" />

        <!-- File list -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_files"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingBottom="80dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

        <!-- Empty state message -->
        <TextView
            android:id="@+id/tv_empty_state"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="No files added yet.\nTap + to add files."
            android:textSize="15sp"
            android:textColor="@color/text_secondary"
            android:visibility="visible" />

        <!-- Send button at bottom -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_send"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_margin="16dp"
            android:text="Send Selected Files"
            android:textSize="16sp"
            app:cornerRadius="12dp"
            app:icon="@drawable/ic_send_file" />

    </LinearLayout>

    <!-- FAB for adding files -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_file"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="80dp"
        android:contentDescription="Add file"
        app:srcCompat="@android:drawable/ic_input_add"
        app:tint="@android:color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
