<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Wi-Fi Direct -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33"/>

    <!-- Location (Required for Wi-Fi discovery on < Android 13) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Storage -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Practice"
        android:usesCleartextTraffic="true">

        <!-- Activities from com.example.practice -->
        <activity android:name=".splash" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".loginActivity" android:exported="true"/>
        <activity android:name=".RegisterActivity" android:exported="false"/>
        <activity android:name=".HomeActivity" android:exported="false"/>
        <activity android:name=".archives.wifi.SendActivity" android:exported="false"/>
        <activity android:name=".ReceiveActivity" android:exported="false"/>
        <activity android:name=".MyFilesActivity" android:exported="false"/>
        <activity android:name=".ResetPassActivity" android:exported="false"/>
        <activity android:name=".BackEndTest" android:exported="true"/>

        <!-- Activities from com.example.practice.archives.wifi -->
        <activity android:name=".archives.wifi.TransferActivity" android:exported="false"/>
        <activity android:name=".archives.wifi.FilePreviewActivity" android:exported="false"/>
        <activity android:name=".archives.wifi.ScanActivity" android:exported="false"/>

        <!-- Activities from com.example.practice.qrshare.activities -->
        <activity android:name=".qrshare.activities.MainActivity" android:label="QR Share Home" />
        <activity android:name=".qrshare.activities.SendActivity" android:label="Send Files" />
        <activity android:name=".qrshare.activities.ReceiveActivity" android:label="Receive Files" />
        <activity android:name=".qrshare.activities.QRScanActivity" android:label="Scan QR Code" />
        <activity android:name=".qrshare.activities.TransferActivity" android:label="Transfer" />
        <activity android:name=".qrshare.activities.QRPairDisplayActivity" android:label="Pairing" />

        <service
            android:name=".qrshare.network.FileTransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>
